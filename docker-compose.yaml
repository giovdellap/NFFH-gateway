services:
  api-gateway:
    build:
      dockerfile: ./Dockerfile
    ports:
      - 8080:8080
    networks:
      - client-be
      - auth
      - farmer-be


networks:
  client-be:
    name: g-client-be
  auth:
    name: g-auth
  farmer-be:
    name: g-farmer-be
